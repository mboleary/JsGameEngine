import { Script } from "jsge-core/src";

const ScriptCollisionCallback = class extends Script {
    /**
     * This function is called when a collision happens
     */
    collisionCallback(a, b) {}
}